let stringToNum = require('../string1.js');
let numberAsString = '-$125.909';
// Expects a number if valid number string is passed, 0 if its not a valid number, and undefined otherwise
console.log(stringToNum(numberAsString));
